import { CardeffectDirective } from './cardeffect.directive';

describe('CardeffectDirective', () => {
  it('should create an instance', () => {
    const directive = new CardeffectDirective();
    expect(directive).toBeTruthy();
  });
});
